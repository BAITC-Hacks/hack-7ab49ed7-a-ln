# Технические детали

Этот документ дополняет [README.md](README.md). Здесь собраны формулы, алгоритмы, схемы данных,
проверки качества и справочник параметров. Все числа получены на предоставленном датасете текущей
версией кода (`uv run moneygraph`).

## Содержание

1. [Архитектура и поток данных](#1-архитектура-и-поток-данных)
2. [Устройство выгрузки](#2-устройство-выгрузки)
3. [Признаки узла](#3-признаки-узла)
4. [Временные признаки](#4-временные-признаки)
5. [Роли: точные правила и формулы](#5-роли-точные-правила-и-формулы)
6. [Обрыв 4-го колена: оценка p_forward](#6-обрыв-4-го-колена-оценка-p_forward)
7. [Приоритет](#7-приоритет)
8. [Кластеры](#8-кластеры)
9. [Дополнительные анализы](#9-дополнительные-анализы)
10. [Схемы выгрузок](#10-схемы-выгрузок)
11. [Экран просмотра](#11-экран-просмотра)
12. [AI-ассистент и локальный сервер](#12-ai-ассистент-и-локальный-сервер)
13. [Масштабирование подробно](#13-масштабирование-подробно)
14. [Проверки качества и воспроизводимость](#14-проверки-качества-и-воспроизводимость)
15. [Справочник параметров](#15-справочник-параметров)

---

## 1. Архитектура и поток данных

Python-пакет `moneygraph` (`src/moneygraph/`), окружение управляется uv. Точка входа —
`moneygraph.cli:main`, у неё три подкоманды: без аргументов или `run` (пайплайн), `explain`, `serve`.

| Шаг | Модуль | Что делает |
|---|---|---|
| 1 | `load.py` | чтение parquet, приведение gid к int64; проверки: пары `edges` = пары `transactions`, суммы и количества совпадают, gid уникальны, все концы рёбер есть в `nodes`; сборка `nx.DiGraph` (изолированные узлы тоже добавляются); базовые метрики — порт стартового кода |
| 2 | `features.py` | структурные признаки, таблица `p_forward` и её бэктест, аномалии, циклы |
| 3 | `temporal.py` | FIFO-оценка сквозного транзита, всплески, поздние поступления, повторяющиеся суммы, маршруты A→B→C |
| 4 | `clusters.py` | Louvain, устойчивость, раскладка для экрана, таблица кластеров с гипотезами |
| 5 | `roles.py` | упорядоченный список правил → `role`, `role_score`, `rule_fired`, `alt_role`, `evidence`, `sink_status` |
| 6 | `priority.py` | флаги, компоненты приоритета, ранг, анализ чувствительности, текст `why` |
| 7 | `extras.py` | таблица циклов, устойчивость сети, запросы данных |
| 8 | `export.py` | CSV (UTF-8 без BOM) и `graph.json` |
| 9 | `viewer.py` | самодостаточный `viewer.html` |
| 10 | `validate.py` | механическая проверка схемы ТЗ; при нарушении пайплайн завершается с ошибкой |

Интерфейсные модули: `cards.py` (карточка узла, поиск по окончанию gid), `assistant/`
(LLM-провайдер, инструменты, агентный цикл), `serve.py` (HTTP-сервер).

Время полного прогона — **около 4,5 с** на обычном ноутбуке. Основные затраты: 10 прогонов Louvain
для устойчивости, точное посредничество (~1 с) и раскладка.

---

## 2. Устройство выгрузки

Прежде чем строить правила, мы проверили, как собран обход. Каждое ребро, найденное на колене `k`,
исходит из узла колена `k−1`:

| колено ребра | колено плательщика 0 | 1 | 2 | 3 |
|---|---|---|---|---|
| 1 | 520 | 0 | 0 | 0 |
| 2 | 0 | 640 | 0 | 0 |
| 3 | 0 | 0 | 1 200 | 0 |
| 4 | 0 | 0 | 0 | 759 |

Следствия:

- **Исходящие полностью известны только у узлов колен 0–3.** Имеются в виду все их
  внутрибанковские переводы ≥5 000 ₸ за июль. У 444 узлов 4-го колена исходящих нет в данных по
  построению (`out_deg = 0` у всех).
- **Входящие неполны у каждого узла:** видны только плательщики из обхода. Поэтому отношение
  «отдал / получил» завышено у всех, а не только у seed. У не-seed узлов, у которых есть и вход, и
  выход (644 узла), медиана отношения 1,07, 75-й перцентиль — 4,0. У 354 узлов отдано больше,
  чем получено.
- Получатель может оказаться на колене меньше, чем колено ребра: деньги возвращаются в уже
  пройденную часть сети. Это видно по 177 взаимным парам и 468 циклам длиной ≤5.

Прочие факты:

- **Seed.** 19 изолированы (нет ни одного ребра), 12 только получают, 23 только отправляют, 27
  делают и то и другое.
- **Слабосвязные компоненты.** Их 16: размеры 1 877, 270, 17, 13, 6, … Ещё 19 изолированных узлов.
  Крупнейшая сильно связная компонента — 85 узлов.
- **Суммы переводов.** Медиана 30 тыс ₸. 65,6% сумм кратны 1 000, 15,6% переводов лежат в диапазоне
  5–10 тыс ₸; это эффект порога выгрузки.

---

## 3. Признаки узла

| Признак | Определение |
|---|---|
| `in_deg`, `out_deg` | число разных плательщиков / получателей |
| `in_kzt`, `out_kzt` | сумма входящих / исходящих (₸) |
| `in_tx`, `out_tx` | число входящих / исходящих переводов |
| `avg_in`, `avg_out` | средний перевод = сумма / число переводов |
| `out_in_ratio` | `out_kzt / in_kzt`; в CSV `-1`, если входящих нет |
| `unseen_inflow_kzt` | `max(0, out_kzt − in_kzt)`: сколько отправлено сверх видимых входящих |
| `seed_in`, `seed_out` | число seed среди плательщиков / получателей |
| `pagerank` | PageRank по направленному графу, вес ребра — `sum_kzt` (из стартового кода) |
| `betweenness` | точное посредничество по направленному графу, **без весов**, нормированное. networkx трактует вес как длину пути, поэтому сумма в качестве веса дала бы обратный смысл |
| `observed_out` | `depth ≤ 3`: исходящие известны полностью |
| `truncated` | `depth = 4` |
| `p_forward` | для колен 0–3 — наблюдаемый факт (1 — пересылал дальше, 0 — нет), для 4-го колена — оценка (раздел 6) |
| `anomaly_z` | робастный z-score относительно узлов того же колена (раздел 9) |
| `typology` | названия типологий AML: типология роли (консолидатор — funnel account (воронка), транзит — pass-through / money mule layering, распределитель — fan-out payouts) плюс rapid movement of funds при флаге `fast_transit`; через `;`, `-` если нет. Колонка `role` сохраняет значения словаря ТЗ |

---

## 4. Временные признаки

Считаются по `transactions.parquet` (`temporal.py`).

**Сквозной транзит, `fast_share`.** Считается только для узлов, у которых есть и входящие, и
исходящие переводы (671 узел). Поступления и отправки объединяются в одну ленту по датам (сначала
поступления дня, затем отправки). Каждая отправка «гасит» остатки поступлений в порядке FIFO, но
только тех, что пришли за 0–2 дня до неё. `fast_share` — доля входящей суммы, погашенная таким
образом. Флаг `fast_transit` ставится при `fast_share ≥ 0,8`; таких узлов 156. В текстах флаг
называется типологией **rapid movement of funds** и добавляется в колонку `typology`.

**Поздние поступления, `late_in_share`.** Доля входящей суммы, пришедшая 30–31 июля. При
`≥ 0,5` у узла без исходящих ставится `sink_status = truncated_time`: деньги могли уйти в августе.

**Всплески.**
- `burst_in` — максимум разных плательщиков за один день; флаг ставится при ≥3 (38 узлов).
- `burst_out` — максимум разных получателей за один день; флаг ставится при ≥5 (42 узла).
- Дата пика попадает в обоснование.

**Повторяющиеся суммы, `repeat_amounts`.** Одна и та же круглая сумма (кратная 1 000) пришла ≥3 раз
минимум от 2 разных плательщиков; таких узлов 24. Дробление ниже 5 000 ₸ не видно, а обилие
переводов на 5–10 тыс. вызвано самим порогом выгрузки. Поэтому сигналом дробления служат именно
повторы.

**Устойчивые маршруты A→B→C** (`routes.csv`). Строится самосоединение транзакций по промежуточному
узлу B: поступление A→B в день d1 и отправка B→C в день d2, где `0 ≤ d2 − d1 ≤ 2` и A ≠ C. Маршрут
считается устойчивым, если повторяется в ≥2 разные даты d1. Найдено 478 маршрутов. Флаг
`repeat_route` получают 76 узлов B.

---

## 5. Роли: точные правила и формулы

Вспомогательные обозначения:

- `ramp(x, lo, hi) = clip((x − lo) / (hi − lo), 0, 1)`;
- `score(s₁, …) = 0,5 + 0,5 · mean(s₁, …)`: 0,5 на пороге, 1,0 при сильном превышении;
- `ratio = out_kzt / in_kzt` (∞, если входящих нет);
- `fast = fast_share` (0, если не считается);
- `late = late_in_share ≥ 0,5`;
- `activity` — перцентиль `log(1 + in_kzt + out_kzt)` среди всех узлов.

Правила проверяются по порядку, срабатывает первое.

### R1 — координатор

Проверяются три условия. Если выполнено несколько, берётся условие с наибольшим score.

| Условие | Формула уверенности | Узлов |
|---|---|---|
| хаб: `in_deg ≥ 8` и `out_deg ≥ 20` | `score(ramp(in_deg, 8, 20), ramp(out_deg, 20, 80))` | 4 |
| платит seed: `seed_out ≥ 3` | `score(ramp(seed_out, 3, 6))` | 3 |
| мост: `betweenness ≥ p99`, `betweenness > 0`, соседи (с самим узлом) лежат в ≥4 разных кластерах, где есть seed | `score(ramp(кластеров, 4, 8))` | 10 |

Итого 17 координаторов. `alt_role` = распределитель, если выполнено условие R2, иначе консолидатор,
если выполнено условие R3.

### R2 / R3 — распределитель и консолидатор

- **Условие R2:** `out_deg ≥ 10` и `out_deg ≥ 3 · max(in_deg, 1)`.
- **Условие R3:** `in_deg ≥ 4` или `seed_in ≥ 2`. Второе условие добавлено для вопроса ТЗ о узлах,
  собирающих деньги нескольких seed; по нему консолидаторами стали 14 узлов.
- **Если выполнены оба**, побеждает бо́льшее из `out_deg / 10` и `in_deg / 4`.
- **Уверенность распределителя:** `score(ramp(out_deg, 10, 60))`.
- **Уверенность консолидатора:** `score(max(ramp(in_deg, 4, 15), ramp(seed_in, 2, 4)))`.
- **`sink_status` консолидатора:**
  - `truncated_depth` — 4-е колено;
  - `not_sink` — есть исходящие;
  - `truncated_time` — исходящих нет, но основные деньги пришли 30–31 июля;
  - иначе `confirmed_sink`.
- **`alt_role` консолидатора:** распределитель, если `out_deg ≥ 10`; конечный получатель, если
  подтверждённый сток.

Правило R3 опирается только на входящие, поэтому применимо и к 4-му колену.

### T4 — 4-е колено (если не сработали R1–R3)

- **Существенность:** `in_kzt ≥ 30 000` или `in_tx ≥ 2`.
- **Несущественно** → периферия, уверенность `1 − activity` (181 узел).
- **`p_forward < 0,5`** → конечный получатель, уверенность `1 − p_forward` (242 узла).
- **`p_forward ≥ 0,5`** → транзит, уверенность `min(p_forward, 0,6)` (21 узел).
- Обоснование всегда содержит «4-е колено, исходящие не выгружены» и долю пересылающих среди
  похожих узлов.

### R4 — транзит

- **R4s, seed с исходящими, у которого входящих нет в выборке** (19 узлов). Уверенность
  `min(score(ramp(out_kzt, 30 000, 1 000 000)), 0,6)`. Обоснование: «входящие вне выгрузки».
- **Общий случай:** есть и вход, и выход, и `ratio ≥ 0,5` или `fast ≥ 0,8`.
  - «Близость к пропуску без удержания» `close`:
    - `1`, если `0,8 ≤ ratio ≤ 1,2`;
    - `max(0, 1 − ln(ratio / 1,2) / ln 10)`, если `ratio > 1,2` (убывает до 0 при ratio = 12);
    - `0,5 · (ratio − 0,5) / 0,3`, если `0,5 ≤ ratio < 0,8`.
  - Уверенность `0,5 + 0,3 · close + 0,2 · fast`. У seed не выше 0,6: входящие seed не наблюдаются.
  - При `ratio > 1,2` обоснование говорит «есть невидимые входящие» (246 узлов).

### R5 — конечный получатель

Условия: исходящие известны (колено ≤3), `in_deg > 0`, `ratio ≤ 0,10`, `in_kzt ≥ 30 000` или
`in_tx ≥ 2`.

- Уверенность: `score(ramp(in_kzt, 30 000, 1 000 000), 1 − ratio / 0,10)`.
- При `late` уверенность умножается на 0,6, и ставится `sink_status = truncated_time`; иначе
  `confirmed_sink`.

### R6 — периферия

Всё остальное. Уверенность `max(0,05; 1 − activity)`: чем меньше оборот, тем увереннее «роли нет».
Подтипы:

- **без данных** — изолированные seed: уверенность 1,0, `sink_status = no_data`;
- **разовое поступление** — нет исходящих, сумма меньше порога существенности;
- **смешанный профиль** — дальше ушло от 10% до 50%.

### Распределение ролей по коленам

| колено | консолидатор | координатор | распределитель | периферия | конечный | транзит |
|---|---|---|---|---|---|---|
| 0 (seed) | 6 | 4 | 5 | 27 | 7 | 32 |
| 1 | 43 | 1 | 10 | 129 | 204 | 85 |
| 2 | 25 | 11 | 17 | 131 | 179 | 99 |
| 3 | 11 | 1 | 12 | 237 | 346 | 182 |
| 4 | 0 | 0 | 0 | 181 | 242 | 21 |

### Обоснование (`evidence`)

- Строится по шаблону роли и всегда содержит числа: контрагенты, переводы, суммы, средний перевод,
  доля и скорость пересылки, число seed среди плательщиков, дата всплеска.
- Суммы форматируются как «254 тыс ₸» / «1,82 млн ₸».
- Ограничение 200 символов соблюдается обрезкой по границам фрагментов «; », без разрыва фраз.
- `rule_fired` дублирует сработавшее условие с фактическими числами, например
  `R3: in_deg=15≥4` или `T4: p_forward=0.26<0.5 (оценка по коленам 1–3)`.

---

## 6. Обрыв 4-го колена: оценка p_forward

**Задача.** Для 444 узлов 4-го колена оценить, пересылают ли они деньги дальше. Их собственные
исходящие неизвестны.

**Метод.**
1. Берём 1 723 не-seed узла колен 1–3 с входящими. Их исходящие известны: 37,4% пересылают дальше.
2. Делим эти узлы на корзины по числу плательщиков (1 / 2 / 3–4 / 5+) и числу входящих переводов
   (1 / 2–4 / 5+).
3. В каждой корзине считаем долю пересылающих со сглаживанием Beta-априором к общей доле:
   `p = (k + 10 · 0,374) / (n + 10)`.
4. Узел 4-го колена получает долю своей корзины.

| плательщиков | переводов | p_forward | узлов в обучении |
|---|---|---|---|
| 1 | 1 | 0,262 | 1 126 |
| 1 | 2–4 | 0,402 | 156 |
| 1 | 5+ | 0,568 | 30 |
| 2 | 2–4 | 0,496 | 195 |
| 2 | 5+ | 0,831 | 33 |
| 3–4 | 2–4 | 0,621 | 83 |
| 3–4 | 5+ | 0,781 | 55 |
| 5+ | 5+ | 0,741 | 45 |

**Почему таблица, а не модель.** Логистическая регрессия на тех же и дополнительных признаках давала
AUC около 0,7 при случайном разбиении. Выигрыш небольшой, а таблицу можно объяснить одной фразой:
«у похожих узлов дальше пересылают 26%».

**Проверка переносимости.**
- Доля пересылающих по коленам стабильна: 0,354 / 0,418 / 0,360.
- Бэктест: таблица, построенная по коленам 1–2, предсказывает колено 3 с **AUC = 0,583**.
- Калибровка бэктеста:

| прогноз | средний прогноз | факт | узлов |
|---|---|---|---|
| ≤ 0,35 | 0,211 | 0,310 | 583 |
| 0,35–0,5 | 0,370 | 0,512 | 80 |
| 0,5–0,65 | 0,547 | 0,420 | 81 |
| > 0,65 | 0,720 | 0,622 | 45 |

**Вывод.** Сигнал слабый, но честно измеренный. Поэтому уверенность у узлов 4-го колена
умеренная, а крупные и транзитные узлы получают запрос выписки в `data_requests.csv`.

Смещение, о котором нужно помнить: входящие узлов 4-го колена приходят только от узлов 3-го колена,
а у узлов колен 1–3 — от любых колен обхода.

---

## 7. Приоритет

```
priority = d · ( 0,35 · w_role(role) · role_score
               + 0,25 · pct(log(1 + in_kzt + out_kzt))
               + 0,20 · max(pct(pagerank), pct(betweenness))
               + 0,10 · min(1, (seed_in + seed_out) / 3)
               + 0,10 · min(1, |сигнальные флаги| / 3) )
d = 0,9 для seed, иначе 1;  результат обрезается до [0, 1]
```

- `pct` — перцентильный ранг среди всех 2 248 узлов.
- `w_role`: координатор 1,0, консолидатор 0,9, распределитель 0,8, транзит 0,6, конечный 0,5,
  периферия 0,1.
- Сигнальные флаги: `fast_transit`, `burst_in`, `burst_out`, `in_cycle`, `repeat_route`,
  `repeat_amounts`, `anomaly`. Флаги `unseen_inflow`, `late_inflow`, `truncated` описывают полноту
  данных и в приоритет не входят.
- Каждое слагаемое (уже умноженное на `d`) сохранено в колонках `prio_role`, `prio_flow`,
  `prio_centrality`, `prio_seed`, `prio_flags`. Их сумма равна `priority_score`.
- Ранг: по убыванию приоритета, при равенстве — по убыванию `in_kzt`, затем по gid.

**Чувствительность.** Каждый из 5 весов по очереди умножаем на 0,8 и на 1,2 с перенормировкой
суммы весов к 1. Это 10 вариантов. Для каждого сравниваем топ-20 с исходным: совпадение
**минимум 85%, в среднем 93%**.

**Новизна.** 17 из топ-20 — не seed (колонка `new_lead` в `top_nodes.csv`).

---

## 8. Кластеры

**Алгоритм.**
- `networkx.louvain_communities` на **неориентированной** проекции графа. Вес пары — сумма переводов
  в обе стороны. Параметры: `resolution = 1,0`, `seed = 42`. Изолированные узлы в алгоритм не
  подаются.
- Нумерация: кластер 0 — 19 изолированных seed. Остальные 71 кластер нумеруются 1..71 по убыванию
  внутреннего оборота.
- Модулярность 0,86. В 9 кластерах больше одного seed. В примечании ТЗ упомянуты 8 устойчивых
  сообществ; расхождение на один кластер зависит от seed Louvain.

**Устойчивость (`stability`).** Louvain запускается ещё 10 раз с seed 0–9. Для кластера в каждом
прогоне находится лучший по Jaccard двойник, и эти значения усредняются.

**Гипотеза.** Собирается из шаблонов по составу ролей, не больше двух фрагментов:

1. есть координатор → «признаки управляющего ядра: координатор … (от N плательщиков, M получателей,
   сумма)»;
2. есть консолидатор (и ≥2 seed или других фрагментов нет) → «признаки funnel account (воронка): …
   собирает от N плательщиков (сумма), в кластере k seed»;
3. есть распределитель → «признаки fan-out payouts: … → N получателей (сумма) — возможны выплаты
   участникам или обналичивание»;
4. транзитных узлов ≥30% → «признаки pass-through / money mule layering: X% узлов пропускают деньги
   дальше — возможен прогон для разрыва следа»;
5. иначе → «периферия: N конечных получателей и M разовых контактов…».

Кластер вне крупнейшей слабосвязной компоненты получает префикс «Изолированный фрагмент».
Кластер 0 описан отдельным шаблоном с запросом иных каналов.

**Раскладка для экрана (`clusters.layout`).** Схема «кластер в кластере»:
- центры кластеров каждой связной части считаются spring layout графа кластеров, где вес — число
  связей между ними;
- круги кластеров радиусом `22·√n + 12` раздвигаются векторизованной процедурой до исчезновения
  перекрытий;
- связные части укладываются рядами, крупнейшая идёт первой;
- внутри кластера применяется spring layout подграфа;
- изолированные seed располагаются сеткой внизу.

Все шаги детерминированы (`seed = 42`).

---

## 9. Дополнительные анализы

**Циклы** (`cycles.csv`). `networkx.simple_cycles(G, length_bound=5)` находит 468 циклов: 177
длины 2, 41 — длины 3, 169 — длины 4, 81 — длины 5. В них участвуют 300 узлов (флаг `in_cycle`).
Таблица отсортирована по минимальной сумме ребра в цикле.

**Аномалии.** Для узлов одного колена считаются робастные z-оценки
`z = 0,6745 · (x − медиана) / MAD`: одна для `log(1 + оборот)`, другая для `log(1 + число переводов)`.
`anomaly_z` — максимум из двух. Флаг ставится при `anomaly_z ≥ 3,5` (40 узлов).

**Устойчивость сети** (`resilience.csv`). Из графа удаляются N узлов с наибольшим приоритетом. Для
сравнения берутся N случайных не-seed узлов, среднее по 20 выборкам, `seed = 42`. Метрики:
- доля размера крупнейшей слабосвязной компоненты от исходной;
- доля оборота на рёбрах, достижимых от seed по направлению переводов;
- число компонент (в исходном графе 35: 16 компонент и 19 изолированных узлов).

| N | крупнейшая компонента: топ / случ. | оборот, достижимый от seed: топ / случ. | компонент: топ / случ. |
|---|---|---|---|
| 5 | 0,910 / 0,997 | 0,869 / 0,998 | 138 / 36,5 |
| 10 | 0,901 / 0,992 | 0,835 / 0,990 | 295 / 39,2 |
| 20 | 0,792 / 0,985 | 0,665 / 0,983 | 413 / 51,8 |
| 50 | 0,570 / 0,955 | 0,263 / 0,948 | 753 / 70,0 |

**Оценка полноты** (`data_requests.csv`). Правила формирования запросов; строки отсортированы по
приоритету узла.

| Правило | Запрос | Строк |
|---|---|---|
| `sink_status = no_data` | наличные / межбанковские операции и переводы < 5 000 ₸ | 19 |
| 4-е колено и (роль консолидатор или транзит, или получено ≥300 тыс ₸) | выписка исходящих за июль–август | 61 |
| `unseen_inflow_kzt ≥ 100 000` и не 4-е колено | входящие от клиентов вне выборки / других банков | 219 |
| `sink_status = truncated_time` | операции за август 2026 | 81 |

---

## 10. Схемы выгрузок

Общие соглашения:
- кодировка UTF-8 без BOM, разделитель — запятая;
- gid — целые (int64);
- пустых значений нет: `-1` в `out_in_ratio` и `fast_share` означает «не применимо», `-` в
  `alt_role` и `flags` — «нет».

**`nodes_roles.csv`** — 2 248 строк, отсортированы по `rank`.

| Колонки | Смысл |
|---|---|
| `gid, role, role_score, cluster_id, priority_score, evidence` | обязательная схема ТЗ |
| `rank` | место по приоритету (1..2248) |
| `rule_fired, alt_role` | сработавшее правило с числами; роль, признаки которой тоже есть |
| `typology` | типологии AML (раздел 3) |
| `sink_status` | `confirmed_sink`, `not_sink`, `truncated_depth`, `truncated_time`, `no_data` |
| `truncated, p_forward` | 4-е колено; факт или оценка пересылки дальше |
| `is_seed, depth` | из `nodes.parquet` |
| `in_deg … out_tx, out_in_ratio, fast_share, unseen_inflow_kzt, seed_in, seed_out, pagerank, betweenness` | метрики (разделы 3–4) |
| `flags` | коды флагов через `;` |
| `prio_role, prio_flow, prio_centrality, prio_seed, prio_flags` | слагаемые приоритета |

**`clusters.csv`.** Сначала обязательные колонки `cluster_id, n_nodes, n_seed, sum_kzt_internal,
top_gids, hypothesis`, где `top_gids` — 5 узлов с наибольшим приоритетом через `;`. Затем
дополнительные:
- `roles_mix` — состав ролей;
- `sum_kzt_in_external`, `sum_kzt_out_external` — потоки из других кластеров и в другие кластеры;
- `stability`;
- `isolated_fragment`;
- `max_priority`.

**`top_nodes.csv`.** Топ-50. Колонки: `rank, gid, role, priority_score, why`, затем
`typology, role_score, is_seed, new_lead, cluster_id, evidence`. Текст `why` состоит из четырёх частей:
- роль с типологией AML, уверенность и правило;
- обоснование;
- сигналы;
- разложение приоритета и гипотеза кластера.

**Дополнительные файлы:**

| Файл | Колонки |
|---|---|
| `routes.csv` | `a, b, c, n_dates, first, last, sum_in, sum_out` |
| `cycles.csv` | `length, path, min_edge_kzt, total_kzt` |
| `resilience.csv` | `removed_n`, `giant_share_*`, `seed_reach_volume_*`, `n_components_*` |
| `data_requests.csv` | `gid, request, reason, node_priority` |
| `p_forward_table.csv` | `in_deg, in_tx, p_forward, n` |

`metrics.json` — сводка прогона: число узлов по ролям, кластеры, модулярность, бэктест
`p_forward`, чувствительность, время прогона.

`graph.json` — данные экрана и ассистента. Контракт описан в
[`docs/graph_json.md`](docs/graph_json.md). В нём все gid — **строки**, потому что 18-значные числа
не помещаются в `Number` JavaScript без потери точности.

---

## 11. Экран просмотра

**Сборка.** `viewer.py` вставляет в шаблон `web/viewer_template.html` библиотеку vis-network 10.1.2
(standalone, 652 КБ) и данные `window.GRAPH = …`. Последовательности `</` в данных экранируются.
Получается один файл около 2,8 МБ без внешних запросов.

**Режим «окрестность»** (по умолчанию):
- плательщики расположены слева, получатели справа, глубина 1 или 2 шага;
- на 2 шагах с каждой стороны остаются 120 самых крупных связей;
- физика отключена, рёбра изогнуты;
- входящие к выбранному узлу и исходящие от него окрашены по-разному.

**Режим «весь граф».** Используются предрасчитанные координаты (раздел 8), физика выключена, рёбра
прямые и скрываются при перетаскивании. Связи выбранного узла дорисовываются поверх.

**Поиск.** Сначала совпадение по окончанию gid, затем по любой части. Работают клавиши ↑↓ и Enter,
`/` ставит фокус в поле поиска. Последних 6 цифр для однозначности мало: они совпадают у 1 358
узлов. Последние 10 цифр уникальны, поэтому метки в текстах используют их.

**Кодирование на схеме:**
- цвет — роль;
- размер — log оборота;
- ромб — seed;
- пунктирная рамка — 4-е колено;
- толщина ребра — log суммы;
- подсказка на ребре — сумма, число переводов и даты.

**Навигация:**
- кнопка «назад»;
- глубокие ссылки `#gid`;
- `window.focusNode(gid)` доступна для внешнего кода (её использует чат).

**Панель чата** появляется, только если страница открыта через `serve` и `GET /api/health` вернул
`ok`. Ответ экранируется, а 18-значные gid превращаются в ссылки.

---

## 12. AI-ассистент и локальный сервер

**Сервер** (`serve.py`, стандартный `ThreadingHTTPServer`, по умолчанию `127.0.0.1:8765`):

| Метод и путь | Ответ |
|---|---|
| `GET /`, `/viewer.html` | экран; 503 с подсказкой, если пайплайн ещё не запускался |
| `GET /graph.json` и другие файлы из `out/` | файлы выгрузки; выход за пределы `out/` заблокирован |
| `GET /api/health` | `{"ok": true, "llm": bool, "provider", "model"}` |
| `POST /api/ask` `{question, focus_gid}` | `{answer, gids, trace, mode, model}` |
| `OPTIONS` | 204 с CORS-заголовками |

Размер тела запроса ограничен 64 КБ (иначе 413). Неверный JSON даёт 400.

**Провайдер** (`assistant/llm.py`).
- Интерфейс `LLMProvider`: `name`, `model`, `chat(messages, tools) → ChatReply(content, tool_calls)`.
  Описания инструментов не зависят от провайдера, так что можно добавить `AnthropicProvider`.
- Реализация `OpenAIProvider` использует Chat Completions с function calling.
- Настройки берутся из `.env`: `OPENAI_API_KEY`, а также необязательные `OPENAI_BASE_URL` и
  `OPENAI_MODEL`.

Если модель не задана, она выбирается по списку `/v1/models`:
- по семействам в порядке `gpt-5` → `gpt-4.1` → `gpt-4o`;
- исключаются аудио, realtime, embedding, search, codex, pro и nano;
- среди оставшихся берётся самая новая версия.

Сейчас выбирается `gpt-5.6-sol`.

Часть моделей gpt-5.x не принимает инструменты вместе с `reasoning_effort` в Chat Completions.
Поэтому провайдер перебирает `low` → `none` → без параметра. При ошибке «модель недоступна» он
переключается на запасной список.

**Агент** (`assistant/agent.py`).
- Системный промпт на русском требует:
  - использовать только данные инструментов;
  - подкреплять каждое утверждение полным gid и числами;
  - формулировать выводы как гипотезы;
  - называть схемы типологиями AML (funnel account, pass-through / money mule layering, fan-out
    payouts, rapid movement of funds);
  - учитывать ограничения данных;
  - завершать ответ 1–3 шагами для аналитика.
- К промпту добавляется сводка графа и узел, на который сейчас смотрит аналитик.
- Выполняется до 6 раундов вызовов инструментов, результат каждого ограничен 12 000 символами.
  Если лимит исчерпан, модель получает просьбу дать итог без инструментов.
- В ответе возвращаются трасса вызовов и список gid из текста.
- Типичное время ответа — около 15 с.

**Инструменты** (`assistant/tools.py`) работают только с `graph.json`:

| Инструмент | Аргументы | Что возвращает |
|---|---|---|
| `overview` | — | период, объёмы, число узлов по ролям, топ-5, ограничения данных |
| `get_node` | `gid_or_suffix` | полная карточка узла |
| `neighbors` | `gid, direction (in/out/both), limit` | контрагенты с суммами, числом переводов и датами |
| `top_nodes` | `role, cluster, n` | узлы по убыванию приоритета |
| `cluster_summary` | `cluster_id` | размер, seed, оборот, гипотеза, состав ролей |
| `find_paths` | `src, dst, max_len` | направленные цепочки с суммами на каждом шаге |
| `search_nodes` | `role, cluster, is_seed, min_in_deg, min_out_deg, min_priority, limit` | поиск по фильтрам |
| `common_counterparties` | `gids, direction, max_hops` | кто связан сразу с несколькими узлами («кто собирает деньги с этих пятерых») |

**Резервный режим.** Без ключа или при ошибке API ассистент ищет в вопросе числа от 4 цифр (или
берёт узел в фокусе), находит по ним узлы и возвращает их карточки. Если узлов нет, возвращает
топ-5. Сервер не падает.

---

## 13. Масштабирование подробно

| Слой | Сейчас (2 248 узлов) | При ~1 млн узлов |
|---|---|---|
| Хранение и признаки | pandas в памяти | DuckDB или Polars поверх parquet: степени, суммы, всплески и повторы — колоночные group-by; FIFO-транзит — оконные функции по (узел, дата) |
| Графовые алгоритмы | networkx | igraph или graph-tool (C/C++); посредничество — приближённое по k выборочным источникам; PageRank — разреженные матрицы |
| Кластеры | Louvain + 10 прогонов для устойчивости | Leiden (leidenalg): быстрее и даёт связные сообщества; устойчивость — на подвыборке кластеров |
| Циклы и маршруты | полный перебор с ограничением длины | внутри слабосвязных компонент параллельно; маршруты — самосоединение транзакций в DuckDB с ограничением по лагу |
| Пороги | абсолютные (≥4 плательщиков и т. п.) | перцентильные, перекалибровка на каждой выгрузке |
| Пересчёт | полный, ~5 с | инкрементальный: пересчитываются только затронутые компоненты |
| Интерфейс | весь граф в одном HTML | API окрестности узла и агрегатов кластеров; раскладка кластеров заранее; рендер на WebGL (Sigma.js) |
| AI-ассистент | инструменты над JSON в памяти | те же инструменты поверх индексов DuckDB; в модель уходит только найденный подграф |

---

## 14. Проверки качества и воспроизводимость

**`validate.py`** запускается в конце каждого прогона. Пайплайн завершается с ошибкой, если
нарушено любое из условий:
- **`nodes_roles.csv`:**
  - ровно 2 248 строк;
  - множество gid совпадает с `nodes.parquet` (чтение как строк, без потери точности);
  - нет дубликатов и пустых значений;
  - роли только из словаря;
  - `role_score` и `priority_score` лежат в [0, 1];
  - `evidence` непустое, не длиннее 200 символов и содержит цифру.
- **`clusters.csv`:**
  - обязательные колонки на месте;
  - каждый `cluster_id` из `nodes_roles` описан;
  - сумма `n_nodes` = 2 248, сумма `n_seed` = 81;
  - гипотезы непустые.
- **`top_nodes.csv`:**
  - не меньше 20 строк;
  - `rank` = 1..N;
  - сортировка по убыванию приоритета;
  - роли и приоритеты совпадают с `nodes_roles`.
- **Все CSV:** без BOM.

**`uv run pytest`** (`tests/test_outputs.py`) — полный прогон во временную папку и проверки:
- схема ТЗ проходит;
- у каждого узла есть роль и обоснование с числами;
- все 444 узла 4-го колена имеют `truncated_depth` и упоминание обрыва в обосновании;
- транзит-seed не получает уверенность выше 0,6;
- `top_nodes` корректен;
- в исходном коде нет 18-значных gid-литералов (защита от захардкоженных ответов).

**Воспроизводимость.**
- Все случайные процедуры зафиксированы (`seed = 42`; для устойчивости — seed 0–9).
- Зависимости закреплены в `uv.lock`.
- Одна команда `uv run moneygraph` воспроизводит все выгрузки из `data/`.

---

## 15. Справочник параметров

Все параметры находятся в `src/moneygraph/config.py` (dataclass `Config`).

| Группа | Параметр | Значение | Смысл |
|---|---|---|---|
| Выгрузка | `max_observed_out_depth` | 3 | исходящие известны для колен ≤3 |
| | `late_days`, `late_share` | 2, 0,5 | «обрыв по времени»: ≥50% входящих за последние 2 дня |
| | `fast_lag_days` | 2 | окно сквозного транзита |
| Координатор | `hub_min_in`, `hub_min_out` | 8, 20 | хаб |
| | `coord_min_seed_payees` | 3 | платит ≥3 seed |
| | `bridge_btw_quantile`, `bridge_min_seed_clusters` | 0,99, 4 | мост |
| Распределитель | `distr_min_out`, `distr_fan_ratio`, `distr_strong_out` | 10, 3, 60 | порог, веерность, насыщение уверенности |
| Консолидатор | `cons_min_in`, `cons_min_seed_payers`, `cons_strong_in` | 4, 2, 15 | порог, seed-плательщики, насыщение |
| Транзит | `transit_lo`, `transit_hi`, `transit_weak_lo` | 0,8, 1,2, 0,5 | окно пропуска и слабая граница |
| | `fast_share_min`, `seed_transit_cap` | 0,8, 0,6 | быстрый транзит; потолок уверенности seed |
| Конечный | `term_min_kzt`, `term_min_tx`, `term_max_out_share`, `late_penalty` | 30 000, 2, 0,10, 0,6 | существенность, удержание, штраф за конец периода |
| 4-е колено | `trunc_forward_cut`, `trunc_transit_cap`, `p_forward_prior` | 0,5, 0,6, 10 | граница, потолок уверенности, сила сглаживания |
| Приоритет | `w_role, w_flow, w_central, w_seed, w_flags` | 0,35, 0,25, 0,20, 0,10, 0,10 | веса слагаемых |
| | `seed_discount`, `top_n` | 0,9, 50 | скидка для seed; длина топ-листа |
| Кластеры | `louvain_seed`, `louvain_resolution`, `stability_runs` | 42, 1,0, 10 | параметры Louvain |
| Сигналы | `burst_in_payers`, `burst_out_recipients` | 3, 5 | всплески за день |
| | `route_min_dates`, `repeat_amount_min` | 2, 3 | устойчивый маршрут; повтор суммы |
| | `anomaly_z`, `cycle_max_len` | 3,5, 5 | порог аномалии; длина цикла |
